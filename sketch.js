function setup() {
  createCanvas(400, 400)
}

function draw() {
  background(220);
 fill ("black");
  let palavra= "MALU";
  textSize(64);
  textAlign(CENTER, CENTER);
text (palavra, 200, 200);

}     
   if (mouseX)
     
   
